# Drizzle migrations — `apps/api`

Generated by `drizzle-kit generate` from the schema in `apps/api/src/db/schema/*`. Apply with
`pnpm --filter api db:migrate` (runs as the `DATABASE_URL` role — the migration/owner role, e.g.
`tm` locally — never the restricted `APP_DATABASE_URL` role the running server uses).

## Rollback runbook

Drizzle-kit does not generate or apply down-migrations automatically — each numbered migration that
needs one ships a hand-authored paired `..._down.sql` file. To roll back:

1. Stop the API server (it must not be serving traffic against a schema its code doesn't expect).
2. Apply the relevant `_down.sql` file(s) by hand, **in reverse numeric order**, via `psql "$DATABASE_URL" -f drizzle/000N_..._down.sql`.
3. Confirm no other migration depends on the tables/columns just dropped before rolling back
   further back than one step.

| Migration | Down file | Notes |
|---|---|---|
| `0000_init_roles_permissions.sql` | `0000_init_roles_permissions_down.sql` | Drops all six tables, reverse FK order. Full teardown of this feature — only run if nothing else references these tables. |

`0001`–`0007` are grants/RLS/seed migrations with no corresponding down files — they're each a
single `GRANT`/`REVOKE`/`CREATE POLICY`/`INSERT` statement, safe to reverse by hand if ever needed
(`REVOKE`/`DROP POLICY`/`DELETE`), but not scripted since nothing in this feature has needed to roll
one back independently of the schema itself.

### Full migration order (reference)

| # | File | What it does |
|---|---|---|
| 0000 | `0000_init_roles_permissions.sql` | Creates all six tables (schema only, no RLS/grants yet). |
| 0001 | `0001_lock_catalog_grants.sql` | `tm_app` grants: catalog read-only, tenant tables full CRUD. |
| 0002–0004 | `000{2,3,4}_rls_*.sql` | Enables + forces RLS on `roles`/`role_permissions`/`user_roles`. |
| 0005–0007 | `000{5,6,7}_seed_*.sql` | Seeds the permission catalog, the four role templates, the one platform Super Admin role. |
| 0008 | `0008_init_tenant_provisioning.sql` | Creates `tenants`, `department_templates`, `departments`, `users` (Tenant Provisioning Core spec). |
| 0009–0011 | `00{9,10,11}_rls_*.sql` | Enables + forces RLS on `tenants`, `departments`, `users` — same idiom as `roles`/`user_roles` (data-model.md `tenants` Isolation). |
| 0012 | `0012_lock_department_catalog_grants.sql` | `tm_app` grants: `department_templates` read-only, `tenants`/`departments`/`users` full CRUD. |
| 0013 | `0013_add_user_roles_users_fk.sql` | **No-op.** A FK from `user_roles.user_id` to `users.id` was attempted and reverted here — it would have blocked the platform Super Admin's role assignment, since `users.tenant_id` is `NOT NULL` and Super Admin isn't tied to any tenant. See research.md §6. |
| 0014 | `0014_seed_provision_tenant_permission.sql` | Seeds the `provision_tenant` permission, granted only to the platform Super Admin role. |
| 0015 | `0015_seed_department_templates.sql` | Seeds the six default department templates. |
| 0016 | `0016_init_super_admin_auth.sql` | Creates `super_admins` and `super_admin_sessions` (Super Admin Authentication spec). Neither has RLS — no `tenant_id` column, no tenant dimension. |
| 0017 | `0017_lock_super_admin_grants.sql` | `tm_app` grants: `super_admins` gets `SELECT`/`UPDATE` only — deliberately **no `INSERT`**, so the running server can never create a Super Admin account (see "Super Admin bootstrap" below). `super_admin_sessions` gets full `SELECT`/`INSERT`/`UPDATE`. |
| 0018 | `0018_rls_tenants_subdomain_lookup.sql` | Additive — adds a second, `SELECT`-only permissive RLS policy to `tenants` (Domain-Based Tenant Routing spec), gated by a narrow `app.subdomain_lookup` flag, authorizing the pre-authentication subdomain→tenant lookup. Does **not** edit `0009_rls_tenants.sql`'s `tenant_isolation` policy, and requires no grant changes (`tm_app` already has full CRUD on `tenants` per `0012`). Uses a plain text comparison (`current_setting(...) = 'true'`), not a `::boolean` cast — a custom GUC referenced at all on a connection returns `''` (not `NULL`) once unset again, and casting `''` to `boolean` throws; a text comparison never does (research.md §2). |
| 0019 | `0019_init_tenant_auth.sql` | Extends `users` with credential columns (`password_hash`, `must_change_password`, `otp_expires_at`, `failed_login_count`, `locked_until`) and creates `tenant_auth_methods`, `user_sessions`, `password_reset_tokens` (Tenant Authentication Configuration spec) — schema only, no RLS/grants yet. |
| 0020 | `0020_rls_tenant_auth.sql` | Enables + forces RLS on the three new tables — the *standard* `tenant_isolation` policy shape (unlike `0018`, no narrow allowance-clause policy is needed: `tenant_id` is always independently resolved from the subdomain *before* these tables are ever queried, research.md §3/§5 of that spec). |
| 0021 | `0021_lock_tenant_auth_grants.sql` | `tm_app` grants: full CRUD on the three new tables. `users`' existing grant (`0012`) already covers this spec's new columns on that table. |
| 0022 | `0022_seed_tenant_auth_permissions.sql` | Seeds `manage_authentication_settings`/`manage_team_members`, granted to the `hr_admin` role template *and* backfilled onto every existing tenant's already-live `hr_admin`-sourced role row — mirrors `0014`'s precedent for backfilling a live row, not just future provisioning. |
| 0023 | `0023_backfill_tenant_auth_methods.sql` | Backfills the default `email_password` method for every tenant provisioned before this migration — otherwise those tenants would have zero enabled login methods. |
| 0024 | `0024_hard_clint_barton.sql` | Department Management spec (009): adds `parent_department_id`, `description`, `status` (+ CHECK) to `departments`; adds `manager_id`/`assistant_manager_id` (both FK → `users.id`) to `departments`; adds `department_id` (FK → `departments.id`) to `users`; replaces the plain `departments_tenant_id_name_unique` constraint with a case-insensitive expression unique index. `departments`/`users` become mutually referencing (research.md §9 of that spec) — safe under Drizzle's lazy `.references()` callback. |
| 0025 | `0025_seed_department_permissions.sql` | Seeds `department.view`/`department.manage`, granted to the `hr_admin` role template *and* backfilled onto every existing tenant's already-live `hr_admin`-sourced role row — mirrors `0014`/`0022`'s precedent. |
| 0026 | `0026_backfill_department_permissions_by_role_name.sql` | Corrective follow-up: `0025`'s backfill only matched roles linked via `source_template_id`, but 511 of 513 "HR/L&D Admin"-named roles in this database have a `NULL` `source_template_id` (not created through `seedDefaultRolesForTenant`) — broadens the backfill to match by role name instead, idempotently (`NOT EXISTS` guard). |
| 0027 | `0027_bouncy_proudstar.sql` | Extensible Custom Fields Framework spec (010): creates `form_definitions` (platform-global, no `tenant_id`), `form_fields` (`tenant_id` nullable — `NULL` = Super-Admin global default), `custom_field_values` (`entity_id` polymorphic, no DB-level FK) — schema only, no RLS/grants yet. |
| 0028 | `0028_rls_custom_fields.sql` | Enables + forces RLS on both new tenant-scoped tables. `custom_field_values` gets the standard single `tenant_isolation` policy. `form_fields` gets this codebase's first *dual-visibility* shape (research.md §1 of that spec): the standard `tenant_isolation` policy, an additive `FOR SELECT`-only `global_fields_readable` policy (mirrors `0018`'s technique) making global (`tenant_id IS NULL`) rows readable by every tenant, and a `super_admin_full_access` policy — the first table to actually use the `app.is_super_admin` allowance clause `platform-auth/super-admin-context.ts` has set on every Super Admin request since that spec shipped, but which no policy had referenced until now. |
| 0029 | `0029_lock_custom_fields_catalog_grants.sql` | `tm_app` grants: `form_definitions` `SELECT`-only (mirrors `permissions`/`department_templates`); `form_fields`/`custom_field_values` full CRUD (RLS enforces the rest). |
| 0030 | `0030_seed_department_form_definition.sql` | Seeds the one `form_definitions` row this spec's demoable flow needs: `key = 'department'`. No `tna` row — Training Needs Analysis has no spec yet. |
| 0031 | `0031_seed_forms_permissions.sql` | Seeds `forms.manage.global`/`forms.manage.tenant`, the latter granted to the `hr_admin` role template *and* backfilled onto every existing tenant role — matching by role name **and** `source_template_id` in the same statement from the start (learning `0025`/`0026`'s two-step lesson in one migration this time). `forms.manage.global` is never granted to any tenant role template — Super Admin only. |
| 0032 | `0032_harden_tenant_isolation_uuid_cast.sql` | Corrective follow-up to `0028`: `tenant_isolation`'s `current_setting('app.tenant_id', true)::uuid` cast (copied from every prior tenant table's policy) throws instead of evaluating to `false` on a pooled connection where `app.tenant_id` was referenced by an earlier, already-ended transaction and isn't set again in the current one — the same class of gotcha `0018`'s own comment documents for `app.subdomain_lookup`, surfaced here by `form_fields`/`custom_field_values`'s new `super_admin_full_access`-exercising transactions, which legitimately never set `app.tenant_id` at all. Wraps the cast in `NULLIF(..., '')` so the empty-string case evaluates to `NULL` (row excluded) instead of throwing. |

## Tenant/department/user RLS bootstrap idiom (research.md §1)

`tenants`, `departments`, and `users` use the exact same tenant-isolation policy shape as `roles`
and `user_roles` (`USING/WITH CHECK (tenant_id = current_setting('app.tenant_id', true)::uuid)`,
or `id = ...` for `tenants` itself, since its own `id` *is* the tenant id). The one difference from
Spec 1: a brand-new tenant's id doesn't exist in the database yet when provisioning starts, so
`apps/api/src/provisioning/provision-tenant.ts` generates the `tenantId` in application code
*before* opening its transaction, then runs `SELECT set_config('app.tenant_id', $1, true)` with that
generated id before the first `INSERT` — so `WITH CHECK` passes for the tenant's own row from the
very first write, with no elevated/`BYPASSRLS` role needed anywhere in this flow.

## Super Admin bootstrap (`super_admins` has no `INSERT` grant for `tm_app`)

Unlike every other table in this schema, `super_admins` cannot be written to by the running server
at all — `0017_lock_super_admin_grants.sql` grants `tm_app` only `SELECT`/`UPDATE`. The only way to
create a Super Admin account is `pnpm --filter api seed:super-admin`
(`apps/api/scripts/seed-super-admin.ts`), a standalone script that connects via `DATABASE_URL` (the
migration/owner role, same as migrations). This makes "Super Admin creation is not a network-reachable
endpoint" a database-level guarantee, not just an application-code convention — see the Super Admin
Authentication spec's research.md §7 and `contracts/seed-super-admin-script.md`.

## Operational notes — connection pooling (research.md §5)

The running server's `pg.Pool` (`APP_DATABASE_URL`, wired in `src/server.ts`) is capped at
`max: 10`. Because the tenant-context plugin (`src/plugins/tenant-context.ts`) holds one dedicated
client per in-flight request for the request's full duration (`SET LOCAL`-equivalent transaction),
pool size directly caps concurrent in-flight requests per server process — under sustained
concurrency above 10, requests queue for a client rather than failing outright. Tune from observed
pool-wait-time metrics once there's real traffic; this is a starting point, not a final number.

**Neon**: always use the pooled (`-pooler` hostname) connection string for `APP_DATABASE_URL` in
staging/production — never the direct string. Neon's lower tiers cap total concurrent connections
well below what an unbounded pool could open; going through Neon's own PgBouncer-based pooler
(transaction-pooling mode) avoids competing with that ceiling. `DATABASE_URL` (migrations) can use
either, since migrations are a short-lived, low-concurrency connection.

## App-role vs. migration-role split

Migrations (`DATABASE_URL`) run as the schema-owning role. The running Fastify server
(`APP_DATABASE_URL`) connects as a separate, restricted, non-superuser role (`tm_app` locally,
created by `drizzle/init/01-app-role.sql` on first container start). This split is required for
`0001_lock_catalog_grants.sql`'s `REVOKE`s to have any real effect — a table owner or superuser
always retains implicit write rights that `GRANT`/`REVOKE` cannot take away.

**Neon (staging/production)**: there is no `docker-entrypoint-initdb.d` equivalent. Before running
migrations against a new Neon project, an operator must create the restricted role by hand once
(adapt `drizzle/init/01-app-role.sql`'s `CREATE ROLE` statement, using a generated password stored
in the deployment's secret manager — never commit a real password), then point `APP_DATABASE_URL`
at that role's pooled connection string.

## Super Admin access checks — no `BYPASSRLS` role

RLS on `roles`/`user_roles` intentionally makes the platform Super Admin role's row unreachable
through `tm_app`'s tenant-scoped connection (FR-007 — no tenant session can ever see it), so
`tm_app` alone can't answer "is this caller the Super Admin" for `GET /admin/permissions`,
`GET /admin/role-templates`, or `POST /provisioning/tenants`. Those three routes use
`requireSuperAdminSession` instead — a dedicated `super_admins` table plus a server-set
`app.is_super_admin` session indicator (`apps/api/src/platform-auth/`). Nothing in this codebase
uses `BYPASSRLS`.

**Consequence**: Super Admin access to these three routes is a binary "does a valid `super_admins`
session exist" check, not a permission-key check — the `view_permission_catalog` and
`provision_tenant` catalog permissions still exist (nothing currently reads them), a leftover from
an earlier model kept rather than deleted, since removing catalog permissions is treated as a
deliberate, separate action (Spec 1 FR-002).
