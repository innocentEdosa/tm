NEXT_PUBLIC_API_URL=http://localhost:3001
# DEV-ONLY: no real auth exists yet. Set to a user id already assigned the platform Super Admin
# role (see apps/api's dev-only x-dev-user-id stub, NODE_ENV=development only) to view
# app/admin/permissions with real data locally. Leave unset otherwise.
DEV_SUPER_ADMIN_USER_ID=
